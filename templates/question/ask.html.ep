<h1>Ask a new question</h1>

<div> 
%= form_for "questionsave" => (enctype => 'multipart/form-data') => begin
    %= label_for title => "Title"
    %= text_field "title"
    %= label_for content => "Content"
    %= text_area "content"
    %= label_for tag => "Tag"
    %= text_field "tag"
    %= label_for file => "File"
    %= file_field "file"
    %= csrf_field
    %= submit_button
% end
</div>

